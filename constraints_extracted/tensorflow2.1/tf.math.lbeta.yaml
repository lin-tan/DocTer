aliases:
- tf.compat.v1.lbeta
- tf.compat.v1.math.lbeta
constraints:
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
  x:
    descp: A rank `n + 1` `Tensor`, `n >= 0` with type `float`, or `double`.
    dtype:
    - float
    - tf.float64
inputs:
  optional:
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/math/lbeta
outputs: The logarithm of \(|Beta(x)|\) reducing along the last dimension.
package: tensorflow
target: lbeta
title: tf.math.lbeta
version: 2.1.0
