aliases:
- tf.compat.v1.decode_compressed
- tf.compat.v1.io.decode_compressed
constraints:
  bytes:
    descp: A `Tensor` of type `string`. A Tensor of string which is compressed.
    dtype:
    - tf.string
    tensor_t:
    - tf.tensor
  compression_type:
    default: ''
    descp: An optional `string`. Defaults to `""`. A scalar containing either (i)
      the empty string (no compression), (ii) "ZLIB", or (iii) "GZIP".
    dtype:
    - tf.string
    enum:
    - GZIP
    - ZLIB
    ndim:
    - '0'
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
inputs:
  optional:
  - compression_type
  - name
  required:
  - bytes
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/io/decode_compressed
outputs: A `Tensor` of type `string`.
package: tensorflow
target: decode_compressed
title: tf.io.decode_compressed
version: 2.1.0
