aliases:
- tf.compat.v1.keras.layers.SpatialDropout3D
check_nan: true
constraints:
  '**kwargs':
    default: null
    descp: ''
  data_format:
    default: None
    descp: '''channels_first'' or ''channels_last''. In ''channels_first'' mode, the
      channels dimension (the depth) is at index 1, in ''channels_last'' mode is it
      at index 4. It defaults to the `image_data_format` value found in your Keras
      config file at `~/.keras/keras.json`. If you never set it, then it will be "channels_last".'
    ndim:
    - '0'
  rate:
    descp: Float between 0 and 1. Fraction of the input units to drop.
    dtype:
    - float
inputs:
  optional:
  - data_format
  - '**kwargs'
  required:
  - rate
layer_constructor: true
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/layers/SpatialDropout3D
package: tensorflow
target: SpatialDropout3D
title: tf.keras.layers.SpatialDropout3D
version: 2.1.0
