aliases:
- tf.compat.v1.keras.preprocessing.image.img_to_array
constraints:
  data_format:
    default: None
    descp: Image data format, either "channels_first" or "channels_last".
    enum:
    - channels_first
    - channels_last
    ndim:
    - '0'
  dtype:
    default: None
    descp: Dtype to use for the returned array.
  img:
    descp: PIL Image instance.
exceptions:
- ValueError: if invalid `img` or `data_format` is passed.
inputs:
  optional:
  - data_format
  - dtype
  required:
  - img
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/preprocessing/image/img_to_array
outputs: A 3D Numpy array.
package: tensorflow
target: img_to_array
title: tf.keras.preprocessing.image.img_to_array
version: 2.1.0
