aliases:
- tf.compat.v1.keras.layers.ZeroPadding1D
check_nan: true
constraints:
  '**kwargs':
    default: null
    descp: ''
  padding:
    default: '1'
    descp: 'Int, or tuple of int (length 2), or dictionary. If int: How many zeros
      to add at the beginning and end of the padding dimension (axis 1).If tuple of
      int (length 2): How many zeros to add at the beginning and at the end of the
      padding dimension (`(left_pad, right_pad)`). '
    dtype:
    - int
    structure:
    - dict
    - tuple
inputs:
  optional:
  - padding
  - '**kwargs'
  required: []
layer_constructor: true
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/layers/ZeroPadding1D
package: tensorflow
target: ZeroPadding1D
title: tf.keras.layers.ZeroPadding1D
version: 2.1.0
