aliases:
- tf.compat.v1.signal.ifftshift
constraints:
  axes:
    default: None
    descp: '`int` or shape `tuple` Axes over which to calculate. Defaults to None,
      which shifts all axes.'
    dtype:
    - int
    ndim:
    - '0'
  name:
    default: None
    descp: An optional name for the operation.
    dtype:
    - tf.string
    ndim:
    - '0'
  x:
    descp: '`Tensor`, input tensor.'
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - axes
  - name
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/signal/ifftshift
outputs: A `Tensor`, The shifted tensor.
package: tensorflow
target: ifftshift
title: tf.signal.ifftshift
version: 2.1.0
