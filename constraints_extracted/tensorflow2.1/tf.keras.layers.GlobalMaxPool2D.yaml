aliases:
- tf.keras.layers.GlobalMaxPooling2D
- tf.compat.v1.keras.layers.GlobalMaxPool2D
- tf.compat.v1.keras.layers.GlobalMaxPooling2D
check_nan: true
constraints:
  '**kwargs':
    default: null
    descp: ''
  data_format:
    default: None
    descp: A string, one of `channels_last` (default) or `channels_first`. The ordering
      of the dimensions in the inputs.`channels_last` corresponds to inputs with shape`(batch,
      height, width, channels)` while `channels_first`corresponds to inputs with shape`(batch,
      channels, height, width)`. It defaults to the `image_data_format` value found
      in your Keras config file at `~/.keras/keras.json`. If you never set it, then
      it will be "channels_last".
    dtype:
    - tf.string
    enum:
    - channels_first
    - channels_last
    ndim:
    - '0'
inputs:
  optional:
  - data_format
  - '**kwargs'
  required: []
layer_constructor: true
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/layers/GlobalMaxPool2D
package: tensorflow
target: GlobalMaxPool2D
title: tf.keras.layers.GlobalMaxPool2D
version: 2.1.0
