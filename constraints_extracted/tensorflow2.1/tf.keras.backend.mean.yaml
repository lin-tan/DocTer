aliases:
- tf.compat.v1.keras.backend.mean
constraints:
  axis:
    default: None
    descp: A list of integer. Axes to compute the mean.
    dtype:
    - int
    structure:
    - list
  keepdims:
    default: 'False'
    descp: A boolean, whether to keep the dimensions or not. If `keepdims` is `False`,
      the rank of the tensor is reduced by 1 for each entry in `axis`. If `keepdims`
      is `True`, the reduced dimensions are retained with length 1.
    dtype:
    - tf.bool
    ndim:
    - '0'
  x:
    descp: A tensor or variable.
    tensor_t:
    - tf.tensor
inputs:
  optional:
  - axis
  - keepdims
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/backend/mean
outputs: A tensor with the mean of elements of `x`.
package: tensorflow
target: mean
title: tf.keras.backend.mean
version: 2.1.0
