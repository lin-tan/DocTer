aliases:
- tf.compat.v1.string_join
- tf.compat.v1.strings.join
constraints:
  inputs:
    descp: A list of at least 1 `Tensor` objects with type `string`. A list of string
      tensors.  The tensors must all have the same shape, or be scalars.  Scalars
      may be mixed in; these will be broadcast to the shape of non-scalar inputs.
    dtype:
    - tf.string
    ndim:
    - '1'
    structure:
    - list
    tensor_t:
    - tf.tensor
  name:
    default: None
    descp: A name for the operation (optional).
    dtype:
    - tf.string
    ndim:
    - '0'
  separator:
    default: ''
    descp: An optional `string`. Defaults to `""`. string, an optional join separator.
    dtype:
    - tf.string
    ndim:
    - '0'
inputs:
  optional:
  - separator
  - name
  required:
  - inputs
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/strings/join
outputs: A `Tensor` of type `string`.
package: tensorflow
target: join
title: tf.strings.join
version: 2.1.0
