aliases:
- tf.compat.v1.compat.forward_compatibility_horizon
constraints:
  day:
    descp: A day (1 <= day <= 31, or 30, or 29, or 28) in month. Must be an`int`.
  month:
    descp: A month (1 <= month <= 12) in year. Must be an `int`.
    dtype:
    - int
  year:
    descp: A year (e.g., 2018). Must be an `int`.
    dtype:
    - int
inputs:
  optional: []
  required:
  - year
  - month
  - day
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/compat/forward_compatibility_horizon
outputs: Nothing.
package: tensorflow
target: forward_compatibility_horizon
title: tf.compat.forward_compatibility_horizon
version: 2.1.0
