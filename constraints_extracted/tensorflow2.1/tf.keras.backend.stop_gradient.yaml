aliases:
- tf.compat.v1.keras.backend.stop_gradient
constraints:
  variables:
    descp: Tensor or list of tensors to consider constant with respect to any other
      variable.
    structure:
    - list
    tensor_t:
    - tf.tensor
inputs:
  optional: []
  required:
  - variables
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/backend/stop_gradient
outputs: A single tensor or a list of tensors (depending on the passed argument) that
  has no gradient with respect to any other variable.
package: tensorflow
target: stop_gradient
title: tf.keras.backend.stop_gradient
version: 2.1.0
