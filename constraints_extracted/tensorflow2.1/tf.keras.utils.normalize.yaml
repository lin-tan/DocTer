aliases:
- tf.compat.v1.keras.utils.normalize
constraints:
  axis:
    default: '-1'
    descp: axis along which to normalize.
    dtype:
    - int
  order:
    default: '2'
    descp: Normalization order (e.g. 2 for L2 norm).
    dtype:
    - int
  x:
    descp: Numpy array to normalize.
    structure:
    - list
inputs:
  optional:
  - axis
  - order
  required:
  - x
link: https://www.tensorflow.org/versions/r2.1/api_docs/python/tf/keras/utils/normalize
outputs: A normalized copy of the array.
package: tensorflow
target: normalize
title: tf.keras.utils.normalize
version: 2.1.0
