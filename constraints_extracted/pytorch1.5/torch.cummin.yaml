constraints:
  dim:
    descp: the dimension to do the operation over
    doc_dtype: int
    dtype:
    - int
    ndim:
    - '0'
  input:
    descp: the input tensor.
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
  out:
    default: None
    descp: the result tuple of two output tensors (values, indices)
    doc_dtype: tuple, optional
    structure:
    - tuple
    tensor_t:
    - torch.tensor
inputs:
  optional:
  - out
  required:
  - input
  - dim
link: https://pytorch.org/docs/1.5.0/torch.html#torch.cummin
package: torch
ret_type: (Tensor, LongTensor)
target: cummin
title: torch.cummin
version: 1.5.0
