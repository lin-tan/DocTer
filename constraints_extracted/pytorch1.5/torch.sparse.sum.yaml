constraints:
  dim:
    default: None
    descp: 'a dimension or a list of dimensions to reduce. Default: reduce over all
      dims.'
    doc_dtype: int or tuple of python:ints
    dtype:
    - int
    ndim:
    - '0'
    structure:
    - list
    - tuple
  dtype:
    default: None
    descp: 'the desired data type of returned Tensor. Default: dtype of `input`.'
    doc_dtype: '`torch.dtype`, optional'
    dtype:
    - torch.dtype
    tensor_t:
    - torch.tensor
  input:
    descp: the input SparseTensor
    doc_dtype: Tensor
    tensor_t:
    - SparseTensor
    - torch.tensor
inputs:
  optional:
  - dim
  - dtype
  required:
  - input
link: https://pytorch.org/docs/1.5.0/sparse.html#torch.sparse.sum
package: torch
target: sum
title: torch.sparse.sum
version: 1.5.0
