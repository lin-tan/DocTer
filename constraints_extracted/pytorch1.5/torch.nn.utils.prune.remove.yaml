constraints:
  module:
    descp: module containing the tensor to prune
    doc_dtype: nn.Module
    tensor_t:
    - torch.tensor
  name:
    descp: parameter name within `module` on which pruning will act.
    doc_dtype: str
    dtype:
    - string
inputs:
  optional: []
  required:
  - module
  - name
link: https://pytorch.org/docs/1.5.0/nn.html#torch.nn.utils.prune.remove
package: torch
target: remove
title: torch.nn.utils.prune.remove
version: 1.5.0
