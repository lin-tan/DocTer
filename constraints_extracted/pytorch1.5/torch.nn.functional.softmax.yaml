check_nan: true
constraints:
  _stacklevel:
    default: '3'
    descp: ''
    dtype:
    - int
    ndim:
    - '0'
  dim:
    default: None
    descp: A dimension along which softmax will be computed.
    doc_dtype: int
    dtype:
    - int
  dtype:
    default: None
    descp: 'the desired data type of returned tensor. If specified, the input tensor
      is casted to `dtype` before the operation is performed. This is useful for preventing
      data type overflows. Default: None.'
    doc_dtype: '`torch.dtype`, optional'
    dtype:
    - torch.dtype
    enum:
    - dtype
    tensor_t:
    - torch.tensor
  input:
    descp: input
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
inputs:
  optional:
  - dim
  - _stacklevel
  - dtype
  required:
  - input
link: https://pytorch.org/docs/1.5.0/nn.functional.html#torch.nn.functional.softmax
package: torch
target: softmax
title: torch.nn.functional.softmax
version: 1.5.0
