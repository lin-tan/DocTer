constraints:
  end_dim:
    default: '-1'
    descp: the last dim to flatten
    doc_dtype: int
    dtype:
    - int
    ndim:
    - '0'
  input:
    descp: the input tensor.
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
  start_dim:
    default: '0'
    descp: the first dim to flatten
    doc_dtype: int
    dtype:
    - int
    ndim:
    - '0'
inputs:
  optional:
  - start_dim
  - end_dim
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.flatten
package: torch
ret_type: Tensor
target: flatten
title: torch.flatten
version: 1.5.0
