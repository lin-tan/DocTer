check_nan: true
constraints:
  inplace:
    default: 'False'
    descp: 'can optionally do the operation in-place. Default: `False`'
    doc_dtype: bool, optional
    dtype:
    - torch.bool
    ndim:
    - '0'
inputs:
  optional:
  - inplace
  required: []
layer_constructor: true
link: https://pytorch.org/docs/1.5.0/nn.html#torch.nn.SELU
package: torch
target: SELU
title: torch.nn.SELU
version: 1.5.0
