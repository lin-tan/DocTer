constraints:
  input:
    descp: the PyTorch tensor to test
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.is_floating_point
package: torch
ret_type: (bool)
target: is_floating_point
title: torch.is_floating_point
version: 1.5.0
