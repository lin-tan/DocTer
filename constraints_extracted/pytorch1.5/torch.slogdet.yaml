constraints:
  input:
    descp: the input tensor of size `(*, n, n)` where `*` is zero or more batch dimensions.
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.slogdet
package: torch
ret_type: (Tensor, Tensor)
target: slogdet
title: torch.slogdet
version: 1.5.0
