constraints:
  device:
    default: None
    descp: selected device. Returns statistics for the current device, given by `current_device()`,
      if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    dtype:
    - int
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.5.0/cuda.html#torch.cuda.memory_stats
package: torch
target: memory_stats
title: torch.cuda.memory_stats
version: 1.5.0
