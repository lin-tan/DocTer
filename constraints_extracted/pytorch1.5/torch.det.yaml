constraints:
  input:
    descp: the input tensor of size `(*, n, n)` where `*` is zero or more batch dimensions.
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.det
package: torch
ret_type: Tensor
target: det
title: torch.det
version: 1.5.0
