check_nan: true
constraints:
  dim:
    default: None
    descp: A dimension along which Softmax will be computed (so every slice along
      dim will sum to 1).
    doc_dtype: int
    dtype:
    - int
inputs:
  optional:
  - dim
  required: []
layer_constructor: true
link: https://pytorch.org/docs/1.5.0/nn.html#torch.nn.Softmax
package: torch
target: Softmax
title: torch.nn.Softmax
version: 1.5.0
