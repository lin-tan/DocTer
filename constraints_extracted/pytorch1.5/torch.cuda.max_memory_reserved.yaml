constraints:
  device:
    default: None
    descp: selected device. Returns statistic for the current device, given by `current_device()`,
      if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    dtype:
    - int
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.5.0/cuda.html#torch.cuda.max_memory_reserved
package: torch
target: max_memory_reserved
title: torch.cuda.max_memory_reserved
version: 1.5.0
