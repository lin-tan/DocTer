constraints:
  gain:
    default: '1'
    descp: optional scaling factor
    dtype:
    - int
    ndim:
    - '0'
  tensor:
    descp: 'an n-dimensional torch.Tensor, where n  >= 2 '
inputs:
  optional:
  - gain
  required:
  - tensor
link: https://pytorch.org/docs/1.5.0/nn.init.html#torch.nn.init.orthogonal_
package: torch
target: orthogonal_
title: torch.nn.init.orthogonal_
version: 1.5.0
