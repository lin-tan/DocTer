constraints:
  device:
    default: None
    descp: selected device. Returns the default `Stream` for the current device, given
      by `current_device()`, if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    dtype:
    - int
    enum:
    - None
    - Stream
    - device
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.5.0/cuda.html#torch.cuda.default_stream
package: torch
target: default_stream
title: torch.cuda.default_stream
version: 1.5.0
