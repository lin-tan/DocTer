constraints:
  dim:
    descp: the dimension in which we index
    doc_dtype: int
    dtype:
    - int
    ndim:
    - '0'
  index:
    descp: the 1-D tensor containing the indices to index
    doc_dtype: LongTensor
    dtype:
    - torch.int64
    tensor_t:
    - torch.tensor
  input:
    descp: the input tensor.
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
  out:
    default: None
    descp: the output tensor.
    doc_dtype: Tensor, optional
    tensor_t:
    - torch.tensor
inputs:
  optional:
  - out
  required:
  - input
  - dim
  - index
link: https://pytorch.org/docs/1.5.0/torch.html#torch.index_select
package: torch
ret_type: Tensor
target: index_select
title: torch.index_select
version: 1.5.0
