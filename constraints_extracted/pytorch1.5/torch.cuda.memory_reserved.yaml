constraints:
  device:
    default: None
    descp: selected device. Returns statistic for the current device, given by `current_device()`,
      if `device` is `None` (default).
    doc_dtype: torch.device or int, optional
    dtype:
    - int
inputs:
  optional:
  - device
  required: []
link: https://pytorch.org/docs/1.5.0/cuda.html#torch.cuda.memory_reserved
package: torch
target: memory_reserved
title: torch.cuda.memory_reserved
version: 1.5.0
