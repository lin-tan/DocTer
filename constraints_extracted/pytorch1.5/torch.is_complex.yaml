constraints:
  input:
    descp: the PyTorch tensor to test
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
inputs:
  optional: []
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.is_complex
package: torch
ret_type: (bool)
target: is_complex
title: torch.is_complex
version: 1.5.0
