constraints:
  input:
    descp: the input tensor of size (*, n, n)  where * is zero or more batch dimensions
    doc_dtype: Tensor
    tensor_t:
    - torch.tensor
  out:
    default: None
    descp: the output tensor.
    doc_dtype: Tensor, optional
    tensor_t:
    - torch.tensor
inputs:
  optional:
  - out
  required:
  - input
link: https://pytorch.org/docs/1.5.0/torch.html#torch.inverse
package: torch
ret_type: Tensor
target: inverse
title: torch.inverse
version: 1.5.0
