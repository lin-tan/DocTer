constraints:
  interp:
    default: '2'
    descp: Interpolation method. See resize_short for details.
    doc_dtype: int, optional, default=2
    dtype:
    - int
    ndim:
    - '0'
  size:
    descp: than the image, then the source image is upsampled to size and returned.
    doc_dtype: Size of the crop formatted as (width, height). If the size is larger
  src:
    descp: ''
    doc_dtype: Source image NDArray
inputs:
  optional:
  - interp
  required:
  - src
  - size
link: https://mxnet.apache.org/versions/1.6/api/python/docs/api/mxnet/image/index.html#mxnet.image.random_crop
package: mxnet
target: random_crop
title: mxnet.image.random_crop
version: 1.6.0
